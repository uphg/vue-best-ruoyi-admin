import presetWind4 from '@unocss/preset-wind4'
import { defineConfig } from 'unocss'

/**
 * UnoCSS 配置
 * @returns {import('unocss').UserConfig} UnoCSS 配置对象
 */
export default defineConfig({
  presets: [
    presetWind4(),
  ],
})
